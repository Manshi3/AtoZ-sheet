BETTER -

#include <bits/stdc++.h> 
int largestElement(vector<int> &arr, int n) {
	sort(largestElement.begin(), largestElement.end());
	return arr(n-1);
}

TC - O(nlogn) for sorting 
SC - O(1) for quixk sort

OPTIMAL -

#include <bits/stdc++.h> 
int largestElement(vector<int> &arr, int n) {
    int largest = arr[0];
    for(int i = 1; i < n; i++) {
        if(arr[i] > largest) largest = arr[i];
    }
    return largest;
}

TC - O(n)
SC - O(1)
